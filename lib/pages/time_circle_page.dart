
import 'package:flutter/material.dart';

class TimeCircles extends StatelessWidget {
  const TimeCircles({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.amber,
    );
  }
}